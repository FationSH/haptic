clear variables; % Remove all variables from the workspace.
close all; % Close all open figure windows.

% Functions

%% Periorismoi
% 10<=q1<=160
% 20<=q2<=170
% 0<=q3<=70 & 290<=q3<=360   0 =(tautizete) 360
l = 0.1;

%% Plot me aples e3hswseis (dokimaste me polles gwnies)
%plot1(l, 45, 135, 45);

%% WorkSpace
%wSpace(l);

%% Statikh
% t = Jt * F    OR   V=J*q' => q'=Jt*V

%  F = [4; 4; 4];
%  J = Jacobian(l,q1,q2,q3);
%  t = Statikh(J, F);
%  disp(' Torque:');
%  disp(t);

%% Dereminant == 0
%det0(l);

%% Max torques
%maxT(l);
